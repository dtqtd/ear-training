# 听见 · 视唱练耳

一个纯前端的视唱练耳应用：随机播放音符序列，通过麦克风检测演唱音高，并按音分偏差逐音评分。

## 运行

在当前目录执行：

```bash
python3 -m http.server 8000
```

然后打开 `http://localhost:8000`。

麦克风权限通常只允许在 `localhost` 或 HTTPS 页面使用，因此不建议直接双击 `index.html`。

## 使用方法

1. 选择音符数量、音域、难度和评分标准。
2. 点击“播放题目”，记住听到的音。
3. 戴上耳机，点击“开始演唱”并允许麦克风权限。
4. 按照听到的乐句，连续、自然地哼出全部音符，不需要在单个音上等待。
5. 唱完后点击“结束并评分”，查看整句音准准确率和逐音偏差。

检测过程中可以随时点击“结束并评分”。输入音量条用于排查麦克风：

- 音量条不动：浏览器没有收到麦克风信号，请检查系统输入设备和输入音量。
- 音量条移动但不显示音名：声音已收到，请靠近麦克风并持续唱一个稳定的“啊”。

音频只在浏览器本地处理，不会录制或上传。
